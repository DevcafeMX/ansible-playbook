---
- name: Install pnpm
  shell: curl -fsSL https://get.pnpm.io/install.sh | bash - # noqa: command-instead-of-shell risky-shell-pipe command-instead-of-module
  tags:
    - install
    - core
    - pnpm
